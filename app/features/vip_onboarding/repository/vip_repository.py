"""
Persistence layer for VIP onboarding feature.

The concrete SQL helpers will live here so the rest of the feature can
remain database-agnostic. For now we only describe the responsibilities.
"""


class VipRepository:
    """Container for future VIP persistence helpers."""

    # NOTE: Real implementations will be added once the schema is ready.
    pass

